{
  "name": "serverless-scheduling-system",
  "version": "1.0.0",
  "description": "Serverless Scheduling System",
  "main": "handler.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1370.0"
  }
}